from scipy.spatial.distance import jensenshannon

def get_vectors(matrix, index):
    cooccurences = np.nonzero(matrix[index])[1]
    np.reshape(np.argpartition(corpus1.collocations[6].toarray(), -1000)[-1000:],(10745,))

def cluster(matrix, vocab):
    pass
